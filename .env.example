# Copy this file to .env on the server and fill in your real values.
# The Django app reads these automatically (python-dotenv).

# --- Database (same MySQL server/credentials as the old static script) ---
DB_HOST=localhost
DB_NAME=db-2026s2e
DB_USER=db-2026s2e
DB_PASSWORD=replace-with-real-password

# --- Django ---
DJANGO_SECRET_KEY=replace-with-a-long-random-string
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=2026s2e.winproject.com.au,127.0.0.1,localhost
